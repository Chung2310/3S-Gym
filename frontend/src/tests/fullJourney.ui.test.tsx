// @vitest-environment jsdom
import '@testing-library/jest-dom/vitest'; import { render, screen } from '@testing-library/react'; import userEvent from '@testing-library/user-event'; import { MemoryRouter, Route, Routes } from 'react-router-dom'; import { ToastProvider } from '../components/ToastProvider'; import FeatureRoute from '../components/FeatureRoute'; import PortalPage from '../pages/PortalPage'; import { FeaturesProvider } from '../services/features'; import { api } from '../services/api';
vi.mock('../services/api', () => ({ api: { get: vi.fn(), post: vi.fn(), patch: vi.fn(), delete: vi.fn() } }));
it('PT đi từ CRM sang Care bằng navigation', async () => { vi.mocked(api.get).mockImplementation(async (path) => { if (path === '/api/features/me') return { data: { CARE: true }, message: '' }; if (path.startsWith('/api/care/today')) return { data: { overdueTasks: [], dueTodayTasks: [], openAlerts: [] }, message: '' }; return { data: [], meta: { page: 1, totalPages: 0 }, message: '' }; }); const user = userEvent.setup(); render(<MemoryRouter initialEntries={['/portal/pt/customers']}><ToastProvider><Routes><Route path="/portal/*" element={<PortalPage session={{ token: 'x', user: { username: 'pt', role: 'PT' } }} />} /></Routes></ToastProvider></MemoryRouter>); expect(await screen.findByRole('heading', { name: 'Khách hàng của tôi' })).toBeVisible(); await user.click(await screen.findByRole('link', { name: 'Chăm sóc' })); expect(await screen.findByRole('heading', { name: 'Việc cần làm hôm nay' })).toBeVisible(); });
it('chặn route khi feature tắt', async () => { render(<MemoryRouter initialEntries={['/private']}><FeaturesProvider initialFeatures={{ CARE: false }}><Routes><Route path="/private" element={<FeatureRoute user={{ username: 'pt', role: 'PT' }} roles={['PT']} feature="CARE"><h1>Private</h1></FeatureRoute>} /></Routes></FeaturesProvider></MemoryRouter>); expect(await screen.findByRole('heading', { name: 'Tính năng chưa khả dụng' })).toBeVisible(); expect(screen.queryByRole('heading', { name: 'Private' })).not.toBeInTheDocument(); });
