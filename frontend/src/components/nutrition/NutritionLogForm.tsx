import { useState, type FormEvent } from 'react'; import { api } from '../../services/api'; import { useToast } from '../ui/ToastProvider'; import { errorMessage } from '../../types';
export default function NutritionLogForm({ customerId, onSaved }: { customerId: string; onSaved: () => void }) { const toast = useToast(); const [form, setForm] = useState({ loggedAt: '', type: 'FOOD', name: '', calories: '' }); const submit = async (event: FormEvent) => { event.preventDefault(); try { const result = await api.post('/api/nutrition/logs', { customerId, ...form, calories: Number(form.calories) }); toast.success(result.message); onSaved(); } catch (error) { toast.error(errorMessage(error)); } }; return <form className="panel" onSubmit={submit}><h2>Nhật ký dinh dưỡng</h2><div className="form-grid"><label className="field"><span>Ngày</span><input type="date" aria-label="Ngày nhật ký" value={form.loggedAt} onChange={(e) => setForm({ ...form, loggedAt: e.target.value })} required /></label><label className="field"><span>Loại</span><select aria-label="Loại nhật ký" value={form.type} onChange={(e) => setForm({ ...form, type: e.target.value })}><option value="FOOD">Thực phẩm</option><option value="ACTIVITY">Vận động</option></select></label><label className="field"><span>Tên</span><input aria-label="Tên món hoặc hoạt động" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} required /></label><label className="field"><span>Calories</span><input type="number" aria-label="Calories nhật ký" value={form.calories} onChange={(e) => setForm({ ...form, calories: e.target.value })} required /></label></div><button className="button button-primary">Lưu nhật ký</button></form>; }
