import type { AchievementDto } from '../../types';
const labels = { MAX_WEIGHT: 'Mức tạ cao nhất', MAX_REPS: 'Số reps cao nhất', MAX_SET_VOLUME: 'Volume set cao nhất', ESTIMATED_1RM: 'Estimated 1RM' } as const;
export default function AchievementList({ achievements }: { achievements: AchievementDto[] }) { return <section className="space-y-3">{achievements.length ? achievements.map((item) => <article className="rounded-xl border border-slate-200 bg-white p-4" key={`${item.exerciseName}-${item.kind}`}><p className="text-xs font-bold uppercase tracking-wide text-secondary">{labels[item.kind]}</p><h3 className="mt-1 font-oswald text-xl font-bold uppercase text-primary">{item.exerciseName}</h3><p className="mt-2 text-2xl font-black text-slate-900">{item.value.toLocaleString('vi-VN')}</p></article>) : <div className="rounded-xl bg-slate-50 p-8 text-center text-sm text-slate-500">Chưa có thành tích.</div>}</section>; }
