import type { JourneyAnalytics } from '../../types';
export default function ProgressOverview({ analytics }: { analytics: JourneyAnalytics }) { const cards = [['Tỷ lệ tham gia', analytics.attendance.rate === null ? '—' : `${analytics.attendance.rate}%`], ['Tổng volume', `${analytics.totalVolume.toLocaleString('vi-VN')} kg`], ['RPE trung bình', analytics.averageRpe ?? '—'], ['Chuỗi tập', `${analytics.streakWeeks} tuần`]]; return <section className="progress-overview"><div className="progress-overview-grid">{cards.map(([label,value]) => <article className="progress-overview-card" key={label}><p className="progress-overview-label">{label}</p><p className="progress-overview-value">{value}</p></article>)}</div>{analytics.dataQuality.reasons.map((reason) => <p className="progress-data-warning" key={reason}>{reason}</p>)}</section>; }
