import { useState } from 'react'; import { api } from '../../services/api'; import { errorMessage } from '../../types'; import { useToast } from '../ui/ToastProvider';
interface Draft { _id: string; summary: string; warnings?: string[]; metrics?: Record<string, number> }
export default function ProgressReportGenerator({ customerId, onSaved }: { customerId: string; onSaved: () => void }) { const toast = useToast(); const [periodStart, setStart] = useState(''); const [periodEnd, setEnd] = useState(''); const [draft, setDraft] = useState<Draft | null>(null); const [summary, setSummary] = useState(''); const [loading, setLoading] = useState(false); const generate = async () => { setLoading(true); try { const result = await api.post<Draft>('/api/progress-reports/generate', { customerId, periodStart, periodEnd }); setDraft(result.data); setSummary(result.data.summary); toast.success(result.message); onSaved(); } catch (error) { toast.error(errorMessage(error)); } finally { setLoading(false); } }; const save = async () => { if (!draft) return; try { const result = await api.patch(`/api/progress-reports/${draft._id}`, { summary }); toast.success(result.message); onSaved(); } catch (error) { toast.error(errorMessage(error)); } }; const publish = async () => { if (!draft) return; try { const result = await api.patch(`/api/progress-reports/${draft._id}/publish`); toast.success(result.message); onSaved(); } catch (error) { toast.error(errorMessage(error)); } }; return <section className="space-y-5 rounded-xl border border-slate-200 bg-white p-4 sm:p-6"><div><h2 className="font-oswald text-2xl font-bold uppercase text-primary">Báo cáo tiến độ</h2><p className="mt-1 text-sm text-slate-600">Tổng hợp tự động từ lịch tập, buổi tập và số đo.</p></div><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-1 text-sm font-semibold text-slate-700"><span>Từ ngày</span><input className="min-h-11 rounded-lg border border-slate-300 px-3" type="date" aria-label="Từ ngày" value={periodStart} onChange={(event) => setStart(event.target.value)} /></label><label className="grid gap-1 text-sm font-semibold text-slate-700"><span>Đến ngày</span><input className="min-h-11 rounded-lg border border-slate-300 px-3" type="date" aria-label="Đến ngày" value={periodEnd} onChange={(event) => setEnd(event.target.value)} /></label></div><button type="button" className="button button-primary" disabled={!periodStart || !periodEnd || loading} onClick={() => void generate()}>{loading ? 'Đang tổng hợp...' : 'Tạo báo cáo tự động'}</button>{draft && <div className="space-y-4 border-t border-slate-200 pt-4">{draft.warnings?.map((warning) => <p className="rounded-lg bg-amber-50 p-3 text-sm text-amber-900" key={warning}>{warning}</p>)}<label className="grid gap-1 text-sm font-semibold text-slate-700"><span>Nội dung báo cáo</span><textarea className="min-h-36 rounded-lg border border-slate-300 p-3" aria-label="Nội dung báo cáo" placeholder="Nội dung báo cáo tiến độ..." value={summary} onChange={(event) => setSummary(event.target.value)} /></label><div className="flex flex-wrap gap-2"><button type="button" className="button button-secondary" onClick={() => void save()}>Lưu chỉnh sửa</button><button type="button" className="button button-primary" onClick={() => void publish()}>Công bố báo cáo</button></div></div>}</section>; }
