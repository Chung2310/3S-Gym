import { useState } from 'react'; import { api } from '../../services/api'; import { errorMessage } from '../../types'; import { useToast } from '../ui/ToastProvider';
interface Draft { _id: string; summary: string; warnings?: string[]; metrics?: Record<string, number> }
export default function ProgressReportGenerator({ customerId, onSaved }: { customerId: string; onSaved: () => void }) { const toast = useToast(); const [periodStart, setStart] = useState(''); const [periodEnd, setEnd] = useState(''); const [draft, setDraft] = useState<Draft | null>(null); const [summary, setSummary] = useState(''); const [loading, setLoading] = useState(false); const generate = async () => { setLoading(true); try { const result = await api.post<Draft>('/api/progress-reports/generate', { customerId, periodStart, periodEnd }); setDraft(result.data); setSummary(result.data.summary); toast.success(result.message); onSaved(); } catch (error) { toast.error(errorMessage(error)); } finally { setLoading(false); } }; const save = async () => { if (!draft) return; try { const result = await api.patch(`/api/progress-reports/${draft._id}`, { summary }); toast.success(result.message); onSaved(); } catch (error) { toast.error(errorMessage(error)); } }; const publish = async () => { if (!draft) return; try { const result = await api.patch(`/api/progress-reports/${draft._id}/publish`); toast.success(result.message); onSaved(); } catch (error) { toast.error(errorMessage(error)); } }; return <section className="panel progress-report"><div className="progress-report-header"><h2 className="progress-form-title">Báo cáo tiến độ</h2><p className="progress-form-description">Tổng hợp tự động từ lịch tập, buổi tập và số đo.</p></div><div className="form-grid progress-report-period"><label className="field"><span>Từ ngày</span><input type="date" aria-label="Từ ngày" value={periodStart} onChange={(event) => setStart(event.target.value)} /></label><label className="field"><span>Đến ngày</span><input type="date" aria-label="Đến ngày" value={periodEnd} onChange={(event) => setEnd(event.target.value)} /></label></div><button type="button" className="button button-primary" disabled={!periodStart || !periodEnd || loading} onClick={() => void generate()}>{loading ? 'Đang tổng hợp...' : 'Tạo báo cáo tự động'}</button>{draft && <div className="progress-report-draft">{draft.warnings?.map((warning) => <p className="progress-data-warning" key={warning}>{warning}</p>)}<label className="field"><span>Nội dung báo cáo</span><textarea className="progress-report-summary" aria-label="Nội dung báo cáo" placeholder="Nội dung báo cáo tiến độ..." value={summary} onChange={(event) => setSummary(event.target.value)} /></label><div className="inline-actions progress-report-actions"><button type="button" className="button button-secondary" onClick={() => void save()}>Lưu chỉnh sửa</button><button type="button" className="button button-primary" onClick={() => void publish()}>Công bố báo cáo</button></div></div>}</section>; }
