import { useEffect, useState, type FormEvent } from 'react'; import { api } from '../../services/api'; import { useToast } from '../ui/ToastProvider'; import { errorMessage } from '../../types';
export interface CalendarItem { _id: string; customerId?: string; title: string; startsAt: string; endsAt: string; notes?: string; status: string }
const local = (value = '') => value ? new Date(value).toISOString().slice(0, 16) : '';
export default function CalendarEventModal({ open, event, onClose, onSaved }: { open: boolean; event?: CalendarItem | null; onClose: () => void; onSaved: () => void }) { const toast = useToast(); const [form, setForm] = useState({ customerId: '', title: '', startsAt: '', endsAt: '', notes: '', status: 'SCHEDULED' }); useEffect(() => { if (open) setForm(event ? { customerId: event.customerId || '', title: event.title, startsAt: local(event.startsAt), endsAt: local(event.endsAt), notes: event.notes || '', status: event.status } : { customerId: '', title: '', startsAt: '', endsAt: '', notes: '', status: 'SCHEDULED' }); }, [event, open]); if (!open) return null; const submit = async (e: FormEvent) => { e.preventDefault(); try { const body = { ...form, ...(form.customerId ? {} : { customerId: undefined }), startsAt: new Date(form.startsAt).toISOString(), endsAt: new Date(form.endsAt).toISOString() }; const result = event ? await api.patch(`/api/calendar-events/${event._id}`, body) : await api.post('/api/calendar-events', body); toast.success(result.message); onSaved(); onClose(); } catch (error) { toast.error(errorMessage(error)); } }; const change = (key: keyof typeof form, value: string) => setForm((current) => ({ ...current, [key]: value })); return <div className="modal-backdrop"><form role="dialog" aria-label={event ? 'Sửa lịch' : 'Tạo lịch'} className="modal" onSubmit={submit}><h2>{event ? 'Sửa lịch' : 'Tạo lịch'}</h2><div className="form-grid"><label className="field"><span>Tên lịch</span><input aria-label="Tên lịch" value={form.title} onChange={(e) => change('title', e.target.value)} required /></label><label className="field"><span>Mã khách hàng</span><input aria-label="Mã khách hàng của lịch" value={form.customerId} onChange={(e) => change('customerId', e.target.value)} /></label><label className="field"><span>Bắt đầu</span><input aria-label="Bắt đầu" type="datetime-local" value={form.startsAt} onChange={(e) => change('startsAt', e.target.value)} required /></label><label className="field"><span>Kết thúc</span><input aria-label="Kết thúc" type="datetime-local" value={form.endsAt} onChange={(e) => change('endsAt', e.target.value)} required /></label><label className="field"><span>Ghi chú</span><textarea aria-label="Ghi chú lịch" value={form.notes} onChange={(e) => change('notes', e.target.value)} /></label></div><div className="modal-actions"><button type="button" className="button button-secondary" onClick={onClose}>Đóng</button><button className="button button-primary">Lưu lịch</button></div></form></div>; }
