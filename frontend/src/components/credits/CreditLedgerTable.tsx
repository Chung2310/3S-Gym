import type { CreditLedgerEntry } from '../../types/credits';
const labels: Record<CreditLedgerEntry['type'], string> = { TOPUP: 'Nạp credit', RESERVE: 'Tạm giữ AI', SETTLE: 'Quyết toán AI', RELEASE: 'Hoàn credit', ADJUSTMENT: 'Điều chỉnh' };
export default function CreditLedgerTable({ entries }: { entries: CreditLedgerEntry[] }) {
  if (!entries.length) return <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-8 text-center text-sm text-slate-500">Chưa có giao dịch credit.</div>;
  return <div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-4 py-3">Thời gian</th><th className="px-4 py-3">Loại</th><th className="px-4 py-3">Thay đổi</th><th className="px-4 py-3">Số dư</th><th className="px-4 py-3">Ghi chú</th></tr></thead><tbody className="divide-y divide-slate-100">{entries.map((entry) => <tr key={entry._id}><td className="whitespace-nowrap px-4 py-3 text-slate-500">{new Date(entry.createdAt).toLocaleString('vi-VN')}</td><td className="px-4 py-3 font-semibold text-slate-800">{labels[entry.type]}</td><td className={`px-4 py-3 font-bold ${entry.availableDelta >= 0 ? 'text-emerald-600' : 'text-rose-600'}`}>{entry.availableDelta > 0 ? '+' : ''}{entry.availableDelta}</td><td className="px-4 py-3 font-semibold">{entry.availableAfter}</td><td className="px-4 py-3 text-slate-500">{entry.reason}</td></tr>)}</tbody></table></div>;
}
