import type { ProgressReport } from '../progress/ProgressReportList'; export default function CustomerProgress({ reports }: { reports: ProgressReport[] }) { return <section className="panel"><h2>Báo cáo tiến độ</h2>{reports.length ? reports.map((report) => <article className="published-card" key={report._id}><h3>{report.summary}</h3><p>{new Date(report.periodStart).toLocaleDateString('vi-VN')} – {new Date(report.periodEnd).toLocaleDateString('vi-VN')}</p></article>) : <div className="empty-state">PT chưa công bố báo cáo tiến độ.</div>}</section>; }
