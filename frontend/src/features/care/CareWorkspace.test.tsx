// @vitest-environment jsdom
import '@testing-library/jest-dom/vitest'; import { render, screen, waitFor } from '@testing-library/react'; import userEvent from '@testing-library/user-event'; import { ToastProvider } from '../../components/ToastProvider'; import { api } from '../../services/api'; import CareWorkspace from './CareWorkspace';
vi.mock('../../services/api', () => ({ api: { get: vi.fn(), post: vi.fn(), patch: vi.fn() } }));
it('chia Today queue và refresh sau complete', async () => { vi.mocked(api.get).mockImplementation(async (path) => path.startsWith('/api/care/today') ? { data: { overdueTasks: [{ _id: 'task-1', title: 'Gọi khách' }], dueTodayTasks: [], openAlerts: [] }, message: '' } : { data: [], message: '' }); vi.mocked(api.patch).mockResolvedValue({ data: {}, message: 'Đã hoàn tất.' }); const user = userEvent.setup(); render(<ToastProvider><CareWorkspace /></ToastProvider>); expect(await screen.findByRole('heading', { name: 'Quá hạn' })).toBeVisible(); await user.click(screen.getByRole('button', { name: 'Hoàn tất Gọi khách' })); await user.type(screen.getByLabelText('Kết quả xử lý'), 'Khách xác nhận quay lại'); await user.click(screen.getByRole('button', { name: 'Xác nhận hoàn tất' })); await waitFor(() => expect(api.get).toHaveBeenCalledTimes(4)); });
