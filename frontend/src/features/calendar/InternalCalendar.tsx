import { useCallback, useEffect, useState } from 'react'; import Pagination from '../../components/Pagination'; import { useToast } from '../../components/ToastProvider'; import { api } from '../../services/api'; import type { PaginationMeta, UserRole } from '../../types'; import { errorMessage } from '../../types'; import CalendarEventModal, { type CalendarItem } from './CalendarEventModal';
export default function InternalCalendar({ role }: { role: UserRole }) { const toast = useToast(); const [items, setItems] = useState<CalendarItem[]>([]); const [meta, setMeta] = useState<PaginationMeta>({ page: 1, totalPages: 0 }); const [dates, setDates] = useState({ fromDate: '', toDate: '' }); const [editing, setEditing] = useState<CalendarItem | null | undefined>(undefined); const load = useCallback(async (page = 1) => { try { const query = new URLSearchParams({ page: String(page), limit: '20' }); if (dates.fromDate) query.set('fromDate', dates.fromDate); if (dates.toDate) query.set('toDate', dates.toDate); const result = await api.get<CalendarItem[]>(`/api/calendar-events?${query}`); setItems(result.data); if (result.meta) setMeta(result.meta); } catch (error) { toast.error(errorMessage(error)); } }, [dates.fromDate, dates.toDate, toast]); useEffect(() => { void load(); }, [load]); const remove = async (item: CalendarItem) => { try { await api.delete(`/api/calendar-events/${item._id}`); void load(meta.page); } catch (error) { toast.error(errorMessage(error)); } }; return <section><div className="section-header"><div><h1>Lịch nội bộ</h1><p>Lịch tập và công việc theo khoảng ngày.</p></div>{role === 'PT' && <button className="button button-primary" onClick={() => setEditing(null)}>Tạo lịch</button>}</div><div className="filter-bar panel"><label className="field"><span>Từ ngày</span><input type="date" aria-label="Lịch từ ngày" value={dates.fromDate} onChange={(e) => setDates({ ...dates, fromDate: e.target.value })} /></label><label className="field"><span>Đến ngày</span><input type="date" aria-label="Lịch đến ngày" value={dates.toDate} onChange={(e) => setDates({ ...dates, toDate: e.target.value })} /></label></div><ul className="panel calendar-list" aria-label="Danh sách lịch">{items.map((item) => <li className="published-card" key={item._id}><strong>{item.title}</strong><span>{new Date(item.startsAt).toLocaleString('vi-VN')}</span><span>{item.status}</span>{role === 'PT' && <div className="inline-actions"><button className="text-button" onClick={() => setEditing(item)}>Sửa</button><button className="text-button text-danger" onClick={() => void remove(item)}>Xóa</button></div>}</li>)}</ul><Pagination page={meta.page || 1} totalPages={meta.totalPages || 0} onPageChange={load} /><CalendarEventModal open={editing !== undefined} event={editing} onClose={() => setEditing(undefined)} onSaved={() => void load(meta.page)} /></section>; }
