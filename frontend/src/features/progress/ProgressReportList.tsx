import DataList, { type DataColumn } from '../../components/DataList'; export interface ProgressReport { [key: string]: unknown; _id: string; status: string; summary: string; periodStart: string; periodEnd: string }
export default function ProgressReportList({ reports }: { reports: ProgressReport[] }) { const columns: DataColumn<ProgressReport>[] = [{ key: 'periodStart', label: 'Từ ngày' }, { key: 'periodEnd', label: 'Đến ngày' }, { key: 'summary', label: 'Tóm tắt' }, { key: 'status', label: 'Trạng thái' }]; return <section className="panel"><h2>Danh sách báo cáo</h2><DataList items={reports} columns={columns} /></section>; }
