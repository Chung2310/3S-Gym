import { useEffect, useState, type FormEvent } from 'react'; import { api } from '../../services/api'; import { useToast } from '../../components/ToastProvider'; import { errorMessage } from '../../types'; import type { KnowledgeDocument } from './KnowledgeBase';
export default function KnowledgeEditor({ document, onSaved, onCancel }: { document?: KnowledgeDocument | null; onSaved: () => void; onCancel: () => void }) { const toast = useToast(); const [form, setForm] = useState({ title: '', topic: '', content: '' }); useEffect(() => { setForm(document ? { title: document.title, topic: document.topic, content: document.content } : { title: '', topic: '', content: '' }); }, [document]); const submit = async (event: FormEvent) => { event.preventDefault(); try { const result = document ? await api.patch(`/api/knowledge/${document._id}`, form) : await api.post('/api/knowledge', form); toast.success(result.message); onSaved(); } catch (error) { toast.error(errorMessage(error)); } }; return <form className="panel" onSubmit={submit}><h2>{document ? 'Sửa tài liệu' : 'Tạo tài liệu'}</h2><div className="form-grid"><label className="field"><span>Tiêu đề</span><input aria-label="Tiêu đề tài liệu" value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} required /></label><label className="field"><span>Chủ đề</span><input aria-label="Chủ đề tài liệu" value={form.topic} onChange={(e) => setForm({ ...form, topic: e.target.value })} required /></label><label className="field"><span>Nội dung</span><textarea aria-label="Nội dung tài liệu" value={form.content} onChange={(e) => setForm({ ...form, content: e.target.value })} required /></label></div><div className="inline-actions"><button className="button button-secondary" type="button" onClick={onCancel}>Hủy</button><button className="button button-primary">Lưu tài liệu</button></div></form>; }
