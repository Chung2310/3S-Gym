// @vitest-environment jsdom
import '@testing-library/jest-dom/vitest'; import { render, screen, waitFor } from '@testing-library/react'; import userEvent from '@testing-library/user-event';
import MeasurementForm from '../../../src/components/progress/MeasurementForm'; import { ToastProvider } from '../../../src/components/ui/ToastProvider'; import { api } from '../../../src/services/api';
vi.mock('../../../src/services/api', () => ({ api: { post: vi.fn() } }));
it('submits all circumferences in the canonical measurements object', async () => { vi.mocked(api.post).mockResolvedValue({ data: {}, message: 'Đã lưu' }); const user = userEvent.setup(); render(<ToastProvider><MeasurementForm customerId="c1" onSaved={vi.fn()} /></ToastProvider>); expect(screen.getByRole('form', { name: 'Ghi số đo' })).toBeVisible(); await user.type(screen.getByLabelText('Ngày đo'), '2026-08-29'); await user.type(screen.getByLabelText('Vòng eo (cm)'), '82'); await user.type(screen.getByLabelText('Bắp chân (cm)'), '38'); await user.click(screen.getByRole('button', { name: 'Lưu số đo' })); await waitFor(() => expect(api.post).toHaveBeenCalledWith('/api/body-measurements', expect.objectContaining({ customerId: 'c1', measurements: expect.objectContaining({ waist: 82, calf: 38 }) }))); });
